# Local-Vault
**Password Protected File Vault** is a fully offline browser-based tool that encrypts any file with a password and stores it securely. Files remain encrypted on disk and are only decrypted temporarily in memory when opened with the correct password. Built with pure HTML, CSS, and JavaScript.
